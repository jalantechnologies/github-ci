name: 'test'
description: 'Run automated test cases'
inputs:
  tag:
    required: true
    description: 'Tag via which docker image will be selected'
runs:
  using: composite
  steps:
    - name: Download artifact
      uses: actions/download-artifact@v2
      with:
        name: image-artifact-${{ inputs.tag }}
        path: /tmp

    - name: Run tests
      shell: bash
      env:
        DOCKER_IMAGE: ${{ inputs.tag }}
      run: |
        docker load --input /tmp/image-${{ inputs.tag }}.tar
        source platform/src/scripts/test.sh
